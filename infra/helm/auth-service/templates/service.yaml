apiVersion: v1
kind: Service
metadata:
  name: {{ include "auth-service.fullname" . }}
spec:
  selector:
    app: auth-service
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
