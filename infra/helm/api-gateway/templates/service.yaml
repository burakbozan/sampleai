apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-gateway.fullname" . }}
spec:
  selector:
    app: api-gateway
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
