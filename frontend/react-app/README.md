# React Frontend (skeleton)

This folder will hold a Create React App / Vite project for the storefront UI. Use React + TypeScript, React Query for data fetching, and component-driven design.

Commands:
- npm install
- npm start
